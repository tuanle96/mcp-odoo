interface:
  display_name: "Mcp Odoo"
  short_description: "Repo-specific patterns and workflows for mcp-odoo"
  default_prompt: "Use the mcp-odoo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true