{
  "comment": "Algorithma vNext: WHERE only. No username/password/api_key on purpose - in request identity mode (ODOO_MCP_IDENTITY_MODE=request) the WHO arrives per request as X-User-Email / X-Odoo-Api-Key. Copy this file, rename the instance and point url/db at the tenant's Odoo.",
  "default": "demo",
  "instances": {
    "demo": {
      "url": "http://127.0.0.1:8069",
      "db": "demo",
      "transport": "xmlrpc",
      "timeout": 30,
      "verify_ssl": true,
      "cross_instance": false,
      "tags": ["algorithma"]
    }
  }
}
